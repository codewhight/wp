function min(a,b)
{
    if(a < b)
        return a;
    else if(b < a)
        return b;
    else
        return a;
}
var num1 = 10, num2 = 30;
console.log("The smaller number of num1 and num2 is", min(num1,num2));

